const ConfiguratorTitle = ({ title }: { title: string }) => {
  return (
    <h1 className="mb-4 text-3xl text-center font-extrabold leading-none tracking-tight text-black">
      {title}
    </h1>
  );
};

export default ConfiguratorTitle;
